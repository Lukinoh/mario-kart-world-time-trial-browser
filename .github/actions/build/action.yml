name: Build the project
description: Run tests and build the project

runs:
  using: composite
  steps:
    - name: Run check (format, lint, types)
      shell: bash
      run: pnpm run check

    - name: Run tests
      shell: bash
      run: pnpm run test

    - name: Build
      shell: bash
      run: pnpm run build
