name: Build the project
description: Run tests and build the project

runs:
  using: composite
  steps:
    - name: Run check (format, lint, types)
      shell: bash
      run: npm run check

    - name: Run tests
      shell: bash
      run: npm run test

    - name: Build
      shell: bash
      run: npm run build
